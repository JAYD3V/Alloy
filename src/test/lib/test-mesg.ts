
const iconCollectionMesg = 'The array-property iconContainer.iconCollections'
   + 'should only contain directories that start with "icon_collection"';

const MESG = {
   ICON_COLLECTION_MESG: iconCollectionMesg
};

export default MESG;
